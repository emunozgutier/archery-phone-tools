import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emunozgutier.archeryphonetools',
  appName: 'Archery Phone Tools',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
